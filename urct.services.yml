services:
  # urct.path_processor:
  #   class: Drupal\urct\PathProcessor\ReferralPathProcessor
  #   tags:
  #     - { name: path_processor_inbound, priority: 200 }
  #     - { name: path_processor_outbound, priority: 400 }
  #   arguments: []
  # urct.path_processor:
  #   class: Drupal\urct\EventSubscriber\ReferralPathSubscriber
  #   tags:
  #     - { name: event_subscriber }
  #   arguments: []
  urct.referral_manager:
    class: Drupal\urct\ReferralManager
    arguments: ['@config.factory', '@page_cache_kill_switch']
    tags:
      - { name: path_processor_inbound, priority: 200 }
      - { name: path_processor_outbound, priority: 400 }
      - { name: event_subscriber }
